import {
	FormRoot as Root,
	type FormRootCommonProps,
	type FormRootOptions,
	type FormRootProps,
	type FormRootRenderProps,
} from "./form-root";

export type {
	FormRootOptions,
	FormRootCommonProps,
	FormRootRenderProps,
	FormRootProps,
};

export { Root };

export const Form = Root;
